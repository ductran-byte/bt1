name: bt1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  intl: ^0.19.0 
  cupertino_icons: ^1.0.8
  shimmer: ^3.0.0
  # ĐÃ THÊM: Gói hiển thị các hiệu ứng hoạt hình Lottie
  lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
